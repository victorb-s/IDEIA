import React from "react";
import {NavContainer, Button} from "./styles";

function NavBotao() {
    return (
        <NavContainer>
        <Button>&lt; </Button>
        <Button> &gt;</Button>
      </NavContainer>
    );
  }

 export default NavBotao;